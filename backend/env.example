# ============================================
# AI Chatbot Support - Environment Variables
# ============================================
# Copy this file to .env.development or .env.production
# Never commit actual .env files to git!

# OpenAI API Configuration
# Required for LLM-powered intent classification and natural language responses
# Get your API key from: https://platform.openai.com/api-keys
# 
# DEVELOPMENT MODE: If LLM_API_KEY is not set, LLM_USE_MOCK will automatically be enabled
# This uses simple keyword matching instead of OpenAI API (no API key needed)
LLM_API_KEY=sk-your-openai-api-key-here
LLM_USE_MOCK=false

# OpenAI Model Selection
# Options: gpt-3.5-turbo (cheaper, faster) or gpt-4-turbo-preview (more accurate)
# Default: gpt-3.5-turbo
LLM_MODEL=gpt-3.5-turbo

# LLM Temperature (0.0 - 2.0)
# Controls randomness: lower = more deterministic, higher = more creative
# Recommended: 0.7 for balanced responses
# Default: 0.7
LLM_TEMPERATURE=0.7

# LLM Max Tokens
# Maximum tokens in response (affects cost and response length)
# Recommended: 500 for chat responses, 200 for summaries
# Default: 500
LLM_MAX_TOKENS=500

# Support Email Address
# Email address used for support team contact and OTP emails
# Default: support@example.com
SENDER_EMAIL=support@example.com

# ============================================
# Notes:
# ============================================
# - LLM_API_KEY is REQUIRED for chatbot functionality
# - If LLM_API_KEY is not set, chatbot will fallback to button-based flow only
# - LLM_MODEL, LLM_TEMPERATURE, and LLM_MAX_TOKENS have sensible defaults
# - SENDER_EMAIL is used in contact_us and OTP email sending

